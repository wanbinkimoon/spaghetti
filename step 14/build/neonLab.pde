
int neons = 100;

// ================================================================

void renderNeons() {
	neons = 
	for (int i = 0; i < neons; ++i) {
		float x1 = 0;
		float x2 = width;
		float y = random(0, height);

		stroke(255);
		float thickness = audioData[1]
		strokeWeight(thickness);
		beginShape();
			vertex(x1, y);
			vertex(x2, y);
		endShape();
	}
}